<% content_for(:title, @shop.shop_name) %>
<div class="container">
  <div class="row">
    <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2">
      <h1><%= t '.title' %></h1>
      <%= form_with model: @shop, url: admin_shop_path(@shop), local: true do |f| %>
        <%= render 'shared/error_messages', object: f.object %>
        <div class="form-group">
          <%= f.label :shop_name %>
          <%= f.text_field :shop_name, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :address %>
          <%= f.text_field :address, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :tel %>
          <%= f.text_field :tel, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :shop_hours %>
          <%= f.text_field :shop_hours, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :price %>
          <div class="input-group">
            <%= f.text_field :price, class: "form-control" %>
            <span class="input-group-text">円</span>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :shop_image %>
          <%= f.file_field :shop_image, onchange: 'previewFileWithId(preview)', class: 'form-control mb-3', accept: 'image/*' %>
          <%= f.hidden_field :shop_image_cache %>
        </div>
        <div class='mt-3 mb-3'>
          <%= image_tag @shop.shop_image.url,
                        id: 'preview',
                        size: '300x200' %>
        </div>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>